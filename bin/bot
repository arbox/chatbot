#!/usr/bin/env ruby
# -*- encoding: utf-8; mode: ruby -*-

require 'yaml'
require 'chatbot'

puts "Starte Chatbot. Beenden mit <Strg-C>"

config = {
	:name => 'Example Chat Bot',
	:jabber_id => 'ldvtestchatbot@googlemail.com',
	:password => 'hundekuc',
	:master => 'dtrierwe@googlemail.com',
	:is_public => true
}

bot = Chatbot::Bot.new(config)

bot.connect